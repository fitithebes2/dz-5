# dz-5
import random

options = ["камінь", "ножиці", "папір", "ящірка", "спок"]

# Хто кого перемагає
rules = {
    "камінь": ["ножиці", "ящірка"],
    "ножиці": ["папір", "ящірка"],
    "папір": ["камінь", "спок"],
    "ящірка": ["спок", "папір"],
    "спок": ["ножиці", "камінь"]
}

print("Гра: Камінь — Ножиці — Папір — Ящірка — Спок")
print("Вибери один із варіантів:", ", ".join(options))

while True:
    player = input("Твій вибір: ").strip().lower()
    if player not in options:
        print("Невірний вибір. Спробуй ще раз.")
        continue

    computer = random.choice(options)
    print(f"Комп'ютер обрав: {computer}")

    if player == computer:
        print("Нічия!")
    elif computer in rules[player]:
        print("Ти переміг! 🎉")
    else:
        print("Ти програв! 😢")

    again = input("Грати ще раз? (так/ні): ").strip().lower()
    if again != "так":
        print("Дякую за гру!")
        break
****
